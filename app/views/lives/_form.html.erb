<%= form_with(model: life, local: true) do |form| %>
  <% if life.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(life.errors.count, "error") %> prohibited this life from being saved:</h2>

      <ul>
      <% life.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :life_title %>
  </div>

  <div class="field">
    <%= form.label :text %>
    <%= form.text_field :text, id: :life_text %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
